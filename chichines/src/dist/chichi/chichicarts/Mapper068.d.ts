import { BaseCart } from "./BaseCart";
export declare class Mapper068Cart extends BaseCart {
    cramStart: number;
    cromStart: number;
    initializeCart(): void;
    setByte(clock: number, address: number, val: number): void;
}
